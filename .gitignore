.ai/
.DS_Store
bin/
# npm CLI ships a JS wrapper under npm/picfast/bin/ (native binary stays ignored)
!npm/picfast/bin/
!npm/picfast/bin/picfast
data/
web-dist/
web/dist/
*.exe
.env

# Local TLS / CA tooling outputs (e.g. mkcert); never commit keys
/ca_cert.json
/ca_cert.pem
/ca_key.pem

# JavaScript — any package dir (web/, mcp/, tools/, …)
node_modules/
mcp/dist/
/picfast
imgapi

# Go test coverage output
coverage.out

# Release translation temp files (local testing)
/release_body_en.md
/translated_release.md
